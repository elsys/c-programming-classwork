#include <stdio.h>
#include <stdlib.h>

struct car_t {
	int id;
	char *brand;
	float engine_power;
	int hp;
};

int main() {

	printf("%d", sizeof(struct car_t));

	car_ptr->door_count;
	(*car_ptr).door_count;

	return 0;
}
